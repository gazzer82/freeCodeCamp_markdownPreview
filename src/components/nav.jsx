import React from 'react';

export default function (props) {
  return(
    <nav>
    <div className="nav-wrapper">
      <a href="#" className="brand-logo center">Markdown Preview</a>
    </div>
  </nav>
  );
}
